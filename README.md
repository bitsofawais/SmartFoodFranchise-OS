# SmartFoodFranchise-OS
A multi-threaded Restaurant Management System built in C. Uses GTK for the frontend UI and POSIX threads, pipes, and mutexes to simulate concurrent backend operations between managers, cooks, and waiters.
